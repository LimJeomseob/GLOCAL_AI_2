"use client";

import { ExpertTeamsView } from "@/components/study/ExpertTeamsView";

/** 탭. 전문가 배정 팀 확인 — 성명·연락처로 배정된 연구모임과 코칭 일정을 연다. */
export default function StudyExpertTeamsPage() {
  return <ExpertTeamsView />;
}
