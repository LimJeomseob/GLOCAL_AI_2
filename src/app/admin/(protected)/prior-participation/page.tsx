"use client";

import { PriorParticipationTable } from "@/components/admin/PriorParticipationTable";

/** 관리자 탭 — 심사기준 1번(프로그램 참여·이수) 근거 대장 */
export default function AdminPriorParticipationPage() {
  return <PriorParticipationTable />;
}
